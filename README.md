# Cat-and-Dog-Detection
This project implements a Convolutional Neural Network (CNN) in TensorFlow/Keras to classify images of cats and dogs. The model is trained on an online dataset and demonstrates key concepts of deep learning, including data preprocessing, model building, and evaluation.
